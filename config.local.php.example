<?php
/**
 * LAKUM Artspace - Local Configuration Template
 * 
 * SECURITY: This file contains sensitive credentials
 * 
 * INSTRUCTIONS:
 * 1. Copy this file to config.local.php
 * 2. Fill in actual values for your environment
 * 3. NEVER commit config.local.php to Git
 * 4. Add config.local.php to .gitignore
 * 5. Upload config.local.php to Hostinger server only
 * 
 * IMPORTANT:
 * - JWT_SECRET must be strong (32+ characters, random)
 * - Database password must be strong
 * - All values must be filled in (no empty strings)
 */

return [
    // ========================================================================
    // DATABASE CONFIGURATION
    // ========================================================================
    'db' => [
        'host'     => 'localhost',              // Hostinger: use localhost
        'user'     => 'u812122863_neama',       // Your database user
        'password' => 'your_secure_password',   // Your database password (CHANGE THIS!)
        'database' => 'u812122863_lakum_artspace', // Your database name
        'port'     => 3306,                     // MySQL port (usually 3306)
        'charset'  => 'utf8mb4',                // Character set (keep as utf8mb4)
    ],

    // ========================================================================
    // SITE CONFIGURATION
    // ========================================================================
    'site' => [
        'url'      => 'https://lakumartspace.com',  // Your site URL (with https)
        'timezone' => 'Asia/Riyadh',                // Your timezone
    ],

    // ========================================================================
    // SECURITY CONFIGURATION
    // ========================================================================
    'security' => [
        // JWT Secret - MUST be strong and unique!
        // Generate with: php -r "echo bin2hex(random_bytes(32));"
        'jwt_secret'        => 'your_strong_jwt_secret_here_32_chars_minimum',
        
        // Session timeout in seconds (3600 = 1 hour)
        'session_timeout'   => 3600,
        
        // Maximum upload size in bytes (5242880 = 5MB)
        'max_upload_size'   => 5242880,
        
        // Allowed file extensions for uploads
        'allowed_extensions' => ['jpg', 'jpeg', 'png', 'gif', 'webp', 'pdf'],
    ],

    // ========================================================================
    // LOGGING CONFIGURATION
    // ========================================================================
    'logging' => [
        // Path to error log file
        'error_log_path' => __DIR__ . '/logs/error.log',
        
        // Display errors in browser (false for production)
        'display_errors' => false,
        
        // Log errors to file (true for production)
        'log_errors'     => true,
    ],

    // ========================================================================
    // UPLOADS CONFIGURATION
    // ========================================================================
    'uploads' => [
        // Directory where uploaded files are stored
        'directory' => __DIR__ . '/uploads/',
        
        // URL where uploaded files are accessible
        'url'       => 'https://lakumartspace.com/uploads/',
    ],
];

/**
 * SECURITY CHECKLIST
 * 
 * Before deploying to production:
 * 
 * ✅ Database Configuration
 *    - [ ] Database host is correct (localhost for Hostinger)
 *    - [ ] Database user is correct
 *    - [ ] Database password is strong and changed from default
 *    - [ ] Database name is correct
 * 
 * ✅ Site Configuration
 *    - [ ] Site URL is correct (https, not http)
 *    - [ ] Timezone is correct
 * 
 * ✅ Security Configuration
 *    - [ ] JWT_SECRET is strong (32+ characters, random)
 *    - [ ] JWT_SECRET is NOT the default value
 *    - [ ] Session timeout is appropriate
 *    - [ ] Max upload size is reasonable
 *    - [ ] Allowed extensions are correct
 * 
 * ✅ Logging Configuration
 *    - [ ] display_errors is false (production)
 *    - [ ] log_errors is true (production)
 *    - [ ] Error log path is writable
 * 
 * ✅ Uploads Configuration
 *    - [ ] Upload directory exists and is writable
 *    - [ ] Upload URL is correct
 * 
 * ✅ File Permissions
 *    - [ ] config.local.php is NOT in Git
 *    - [ ] config.local.php is in .gitignore
 *    - [ ] config.local.php permissions are 600 (owner only)
 *    - [ ] logs/ directory permissions are 775
 *    - [ ] uploads/ directory permissions are 775
 * 
 * ✅ Production Deployment
 *    - [ ] config.local.php is uploaded to Hostinger
 *    - [ ] Database connection works
 *    - [ ] Error logs are accessible
 *    - [ ] Uploads directory is writable
 *    - [ ] Site is accessible via HTTPS
 */
?>