name: Deploy to Hostinger (Simple)

on:
  push:
    branches:
      - main
      - master
      - deployment-hostinger

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /public_html/
          local-dir: ./
          state-name: .ftp-deploy-sync-state.json
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
            **/.env*
            **/database.local.php
            **/logs/**
            **/cache/**
            **/tmp/**
            **/*.md
            **/.DS_Store
            **/Thumbs.db
            **/.vscode/**
            **/.idea/**
            **/tests/**
            **/*.bak
            **/*.backup
            **/vendor/**
            **/composer.lock
            **/.ftp-deploy-sync-state.json
            **/eventfile/**
            **/uploads/**
            **/assest/blog-uploads/**
            **/assest/press-uploads/**

      - name: Notify Deployment Complete
        if: success()
        run: echo "✓ Deployment successful to Hostinger"

      - name: Notify Deployment Failed
        if: failure()
        run: echo "✗ Deployment failed - check FTP credentials"
