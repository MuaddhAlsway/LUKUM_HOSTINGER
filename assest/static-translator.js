/**
 * DISABLED - Static Translator
 * PHASE 1: Disabled JSON-based translation
 * 
 * This file is kept for reference but all functionality is disabled.
 * It was never used in the active system anyway.
 * 
 * Original purpose: Load translations from JSON files
 * Current status: DISABLED - No translation happening
 * 
 * PHASE 2 will introduce a new translation system.
 */

// Stub to prevent errors if code references this
window.staticTranslator = {
    init: function() { },
    applyLanguage: function(lang) { },
    get: function(key, defaultValue) { return defaultValue; },
    translateElements: function() { }
};

console.log('Static Translator: DISABLED (PHASE 1 cleanup)');
