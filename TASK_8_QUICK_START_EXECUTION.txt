================================================================================
TASK 8: DATABASE MIGRATION - QUICK START EXECUTION GUIDE
================================================================================

STATUS: Ready to Execute
TIME: 5 minutes
RISK: Very Low

================================================================================
WHAT YOU NEED TO DO
================================================================================

1. Connect to Hostinger database
2. Run SQL statements (copy-paste ready)
3. Verify success
4. Done!

================================================================================
STEP 1: CONNECT TO HOSTINGER
================================================================================

Option A: SSH + MySQL Command Line
-----------------------------------
ssh u812122863@lakumartspace.infinityfree.me
mysql -h localhost -u u812122863_neama -p u812122863_lakum_artspace
Password: Nema202610!LakumDB

Option B: phpMyAdmin (Web Interface)
-------------------------------------
1. Go to Hostinger control panel
2. Find phpMyAdmin
3. Select database: u812122863_lakum_artspace
4. Go to SQL tab
5. Paste SQL statements below

================================================================================
STEP 2: RUN SQL STATEMENTS (Copy-Paste Ready)
================================================================================

COPY EVERYTHING BELOW AND PASTE INTO YOUR MYSQL CLIENT:

-- ============================================
-- PRIORITY 1: Add Missing Columns
-- ============================================
ALTER TABLE `event_gallery`
ADD COLUMN `caption_en` varchar(255) DEFAULT NULL AFTER `caption`,
ADD COLUMN `caption_ar` varchar(255) DEFAULT NULL AFTER `caption_en`;

ALTER TABLE `blog_gallery`
ADD COLUMN `caption` varchar(255) DEFAULT NULL AFTER `image_url`,
ADD COLUMN `caption_en` varchar(255) DEFAULT NULL AFTER `caption`,
ADD COLUMN `caption_ar` varchar(255) DEFAULT NULL AFTER `caption_en`;

-- ============================================
-- PRIORITY 2: Create Missing Table
-- ============================================
CREATE TABLE IF NOT EXISTS `press_gallery` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `press_id` int(11) NOT NULL,
  `image_url` varchar(500) NOT NULL,
  `caption` varchar(255) DEFAULT NULL,
  `caption_en` varchar(255) DEFAULT NULL,
  `caption_ar` varchar(255) DEFAULT NULL,
  `display_order` int(11) DEFAULT 0,
  `created_at` timestamp NULL DEFAULT current_timestamp(),
  PRIMARY KEY (`id`),
  KEY `idx_press_id` (`press_id`),
  CONSTRAINT `fk_press_gallery_press` FOREIGN KEY (`press_id`) REFERENCES `press` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ============================================
-- PRIORITY 3: Add Foreign Keys
-- ============================================
ALTER TABLE `event_gallery` 
ADD CONSTRAINT `fk_event_gallery_event` 
FOREIGN KEY (`event_id`) REFERENCES `events` (`id`) ON DELETE CASCADE;

ALTER TABLE `blog_gallery` 
ADD CONSTRAINT `fk_blog_gallery_blog` 
FOREIGN KEY (`blog_id`) REFERENCES `blogs` (`id`) ON DELETE CASCADE;

ALTER TABLE `blog_translations` 
ADD CONSTRAINT `fk_blog_translations_blog` 
FOREIGN KEY (`blog_id`) REFERENCES `blogs` (`id`) ON DELETE CASCADE;

ALTER TABLE `event_translations` 
ADD CONSTRAINT `fk_event_translations_event` 
FOREIGN KEY (`event_id`) REFERENCES `events` (`id`) ON DELETE CASCADE;

ALTER TABLE `press_translations` 
ADD CONSTRAINT `fk_press_translations_press` 
FOREIGN KEY (`press_id`) REFERENCES `press` (`id`) ON DELETE CASCADE;

-- ============================================
-- PRIORITY 4: Add Performance Indexes
-- ============================================
ALTER TABLE `event_gallery` ADD INDEX `idx_event_id` (`event_id`);
ALTER TABLE `blog_gallery` ADD INDEX `idx_blog_id` (`blog_id`);
ALTER TABLE `press_gallery` ADD INDEX `idx_press_id` (`press_id`);

ALTER TABLE `event_translations` ADD INDEX `idx_event_id` (`event_id`);
ALTER TABLE `event_translations` ADD INDEX `idx_language` (`language`);
ALTER TABLE `blog_translations` ADD INDEX `idx_blog_id` (`blog_id`);
ALTER TABLE `blog_translations` ADD INDEX `idx_language` (`language`);
ALTER TABLE `press_translations` ADD INDEX `idx_press_id` (`press_id`);
ALTER TABLE `press_translations` ADD INDEX `idx_language` (`language`);

================================================================================
STEP 3: VERIFY SUCCESS
================================================================================

Run these verification queries:

-- Check event_gallery columns
SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS 
WHERE TABLE_NAME = 'event_gallery' AND TABLE_SCHEMA = 'u812122863_lakum_artspace'
ORDER BY ORDINAL_POSITION;

Expected result: Should show caption, caption_en, caption_ar columns

-- Check blog_gallery columns
SELECT COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS 
WHERE TABLE_NAME = 'blog_gallery' AND TABLE_SCHEMA = 'u812122863_lakum_artspace'
ORDER BY ORDINAL_POSITION;

Expected result: Should show caption, caption_en, caption_ar columns

-- Check press_gallery exists
SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_NAME = 'press_gallery' AND TABLE_SCHEMA = 'u812122863_lakum_artspace';

Expected result: Should show press_gallery

================================================================================
STEP 4: TEST YOUR WEBSITE
================================================================================

1. Open https://lakumartspace.infinityfree.me/spaces.php
   ✓ Should see past exhibitions carousel

2. Open https://lakumartspace.infinityfree.me/blog.php
   ✓ Should see blog cards

3. Open https://lakumartspace.infinityfree.me/press.php
   ✓ Should see press cards

4. Open DevTools (F12) → Network tab
   ✓ Should see API calls with 200 status

5. Click EN/AR language switcher
   ✓ Should switch language correctly

================================================================================
TROUBLESHOOTING
================================================================================

Problem: "Column already exists" error
Solution: This is OK - means column was already added. Continue.

Problem: "Foreign key constraint fails" error
Solution: Check if all IDs in gallery tables exist in parent tables.
         Run: SELECT COUNT(*) FROM event_gallery WHERE event_id NOT IN (SELECT id FROM events);

Problem: "Duplicate key name" error
Solution: This is OK - means index already exists. Continue.

Problem: Still seeing "LanguageManager not ready" messages
Solution: 
  1. Clear browser cache (Ctrl+Shift+Delete)
  2. Hard refresh (Ctrl+Shift+R)
  3. Verify spaces.php was deployed correctly

Problem: API calls still failing
Solution:
  1. Verify database migrations ran successfully
  2. Check error logs: tail -f /home/u812122863/logs/error.log
  3. Verify database connection in config.local.php

================================================================================
WHAT CHANGED
================================================================================

BEFORE:
  ✗ event_gallery: No caption_en, caption_ar columns
  ✗ blog_gallery: No caption columns at all
  ✗ press_gallery: Table doesn't exist
  ✗ No foreign keys
  ✗ No indexes

AFTER:
  ✓ event_gallery: Has caption, caption_en, caption_ar
  ✓ blog_gallery: Has caption, caption_en, caption_ar
  ✓ press_gallery: Table created with all columns
  ✓ Foreign keys in place
  ✓ Indexes created for performance

================================================================================
EXPECTED RESULTS
================================================================================

✓ Spaces page: Past exhibitions carousel displays events
✓ Blog page: Blog cards display correctly
✓ Press page: Press cards display correctly
✓ Calendar page: Events display correctly
✓ Language switching: Works correctly (EN/AR)
✓ Console: Clean (no errors)
✓ Network: All API calls successful (200 status)

================================================================================
SUMMARY
================================================================================

Task: Database Migration
Status: Ready to Execute
Time: 5 minutes
Risk: Very Low
Impact: Enables bilingual gallery captions and press galleries

Next Steps:
1. Connect to database
2. Copy-paste SQL statements above
3. Run verification queries
4. Test website
5. Done!

================================================================================
NEED HELP?
================================================================================

Read these files for more details:
- TASK_8_DATABASE_MIGRATION_ACTION_PLAN.md (detailed guide)
- DATABASE_ANALYSIS_REPORT.md (technical analysis)
- SQL_STATEMENTS_BY_USE_CASE.sql (all SQL options)
- COMPLETE_PROJECT_STATUS_SUMMARY.md (project overview)

================================================================================

